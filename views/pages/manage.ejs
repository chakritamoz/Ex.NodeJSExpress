<!DOCTYPE html>
<html lang="en">
  <head>
      <%- include("../partials/head.ejs"), {title: title} %> 
  </head>
  <body>
    <header>
        <%- include("../partials/header.ejs") %>
    </header>
    <div class="container">
      <h2>จัดการสินค้า</h2>
      <p>เพิ่ม ลบ แก้ไขข้อมูลในฐานข้อมูล</p>            
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>ชื่อสินค้า</th>
            <th>ราคา</th>
            <th>รายละเอียด</th>
            <th>ลบ</th>
            <th>แก้ไข</th>
          </tr>
        </thead>
        <tbody>
          <% products.forEach(item => { %>
            <tr>
              <td><%= item.name %></td>
              <td><%= item.price %></td>
              <td><%= item.description %></td>
              <td><a class="btn btn-danger" href="/delete/<%= item._id %>" onclick="return confirm('คุณต้องการลบข้อมูลใช่หรือไม่')">ลบ</a></td>
              <td>
                <form action="/edit" method="post">
                  <input type="hidden" name="prod_id" value="<%= item._id %>"/> 
                  <button type="submit" class="btn btn-warning">แก้ไข</button>
                </form>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <footer>
      <%- include('../partials/footer.ejs') %>
    </footer>
  </body>
</html>
